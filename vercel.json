{
  "framework": "vite",
  "buildCommand": "npx vite build",
  "devCommand": "npx vite",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
